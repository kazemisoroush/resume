# Generated outputs — the single source of truth is content.yaml (+ build.py).
# Run `python build.py` to regenerate these locally.
/index.html
/resume-print.html
/resume.pdf

# Legacy LaTeX artifacts (no longer used)
/resume.tex
resume_replaced.*
*.aux
*.log
*.out

# Site assembly
_site/

# Editor
.idea
